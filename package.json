{
    "scripts": {
        "dev": "live-server build --no-browser",
        "sass": "sass --no-source-map"
    },
    "devDependencies": {
        "live-server": "^1.2.2",
        "sass": "^1.50.0"
    }
}
